<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="apollo.app.wofang"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name="apollo.app.wofang.WoFangApplication"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="apollo.app.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="duration"
                android:value="1200" />
        </activity>
        <activity android:name="apollo.app.wofang.home.MainActivity" />
        <activity android:name="apollo.app.wofang.home.TestActivity" />
    </application>

</manifest>